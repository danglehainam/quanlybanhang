/// Centralized route name constants.
/// All navigation code MUST use these instead of hardcoded strings.
class AppRoutes {
  AppRoutes._();

  static const String login = '/login';
  static const String register = '/register';
  static const String home = '/home';
}
